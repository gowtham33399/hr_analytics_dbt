[project]
name = "hr-analytics-dbt"
version = "0.1.0"
description = "This is my first DBT Project"
readme = "README.md"
authors = [
    { name = "gowtham33399", email = "gowthamrama97@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dbt-core>=1.12.2",
    "dbt-snowflake>=1.12.0",
]

[project.scripts]
hr-analytics-dbt = "hr_analytics_dbt:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
